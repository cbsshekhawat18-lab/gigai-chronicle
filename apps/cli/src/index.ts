/**
 * gigai-chronicle — The chronicle CLI — every core capability, headless (ARCHITECTURE.md §14).
 *
 * M1 scaffold (docs/issues/001): this package intentionally exports only its
 * identity until its owning milestone (M4, M6, M8) lands real modules.
 */

/** Canonical package name; kept in sync with package.json by test. */
export const PACKAGE_NAME = "gigai-chronicle";
