/**
 * gigaichronicle-vscode — VS Code / Cursor / Windsurf extension: Sessions tree + Timeline webview (ARCHITECTURE.md §15).
 *
 * M1 scaffold (docs/issues/001): this package intentionally exports only its
 * identity until its owning milestone (M10) lands real modules.
 */

/** Canonical package name; kept in sync with package.json by test. */
export const PACKAGE_NAME = "gigaichronicle-vscode";
