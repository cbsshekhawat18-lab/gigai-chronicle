/**
 * @gigaichronicle/schema — Chronicle Spec artifacts: ChronicleEvent JSON Schemas, zod definitions, generated types (ARCHITECTURE.md §5).
 *
 * M1 scaffold (docs/issues/001): this package intentionally exports only its
 * identity until its owning milestone (M2) lands real modules.
 */

/** Canonical package name; kept in sync with package.json by test. */
export const PACKAGE_NAME = "@gigaichronicle/schema";
