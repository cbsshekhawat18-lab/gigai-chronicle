/**
 * @gigaichronicle/core — Chronicle engine: Event Engine, Chronicle Store, Replay Engine, correlation, redaction, query API (ARCHITECTURE.md §8–§11).
 *
 * M1 scaffold (docs/issues/001): this package intentionally exports only its
 * identity until its owning milestone (M3–M5, M8, M9) lands real modules.
 */

/** Canonical package name; kept in sync with package.json by test. */
export const PACKAGE_NAME = "@gigaichronicle/core";
